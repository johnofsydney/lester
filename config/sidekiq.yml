:concurrency: 2 # delibertely low to preserve CPU and memory on low spec server
:queues:
  - [critical, 5]
  - [default, 2]
  - [low, 1]

:scheduler:
  :schedule:
    # hard_job:
    #   cron: '0 * * * * *'
    #   class: HardJob
    ingest_contracts_date:
      cron: '0 2 * * *' # every day ay 2am
      class: IngestContractsDateJob