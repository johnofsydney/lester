<%= render MenuComponent.new %>

<h1>Editing Transfer</h1>

<%= render "form", transfer: @transfer %>

<br>

<div>
  <%= link_to "Show this transfer", @transfer %> |
  <%= link_to "Back to transfers", transfers_path %>

  <%= button_to "Destroy this transfer", @transfer, method: :delete, data: { confirm: "Are you sure?" }, class: "button is-danger" %>
</div>
